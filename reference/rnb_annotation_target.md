# rnb_annotation_target

Resolve the annotation target of an RnBeads object.

## Usage

``` r
rnb_annotation_target(rnb_set)
```

## Arguments

- rnb_set:

  An `RnBSet` object.

## Value

A character scalar naming the annotation target, `"sites"` for
sequencing sets and the array platform for array sets.
