# helper function to compute row-wise z-score of a matrix

This function takes a matrix and computes row-wise z-scores.

## Usage

``` r
computeRowZScore(mat)
```

## Arguments

- mat:

  A matrix

## Value

A matrix with row-wise z-scores
