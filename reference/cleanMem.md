# cleanMem

cleanMem is a function to clean the memory

## Usage

``` r
cleanMem(iter.gc = 1L)
```

## Arguments

- iter.gc:

  - number of times to run the garbage collector

## Value

invisible NULL
